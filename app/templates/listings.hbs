<div>
    <label>Edit Listing ({{total}})</label>
    {{#each}}
        <div>
            {{#link-to 'listing' this}}
                {{id}}: {{name}}
            {{/link-to}}
        </div>

    {{/each}}
</div>
